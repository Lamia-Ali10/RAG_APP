version: "3.9"
services:
  backend:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - secret.env

  frontend:
    build: ./static
    ports:
      - "80:80"
    depends_on:
      - backend